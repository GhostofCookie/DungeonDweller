#!/bin/bash

FNAME=$(date +%y%m%d) 
TO="Menu GameState Room RoomTree Image ImageImporter Puzzles Cutscene"

cd "/home/rigt2720/Kodika"
echo "Pulling Git Repository"
git pull  

if [ $? -eq 0 ]
then
    for d in $TO
    do
	(cd "$d" && make clean-all)
	(cd "$d" && make )
    done

    cd -
fi
